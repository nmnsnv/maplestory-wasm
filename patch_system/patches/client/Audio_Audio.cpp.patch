diff --git a/Audio/Audio.cpp b/Audio/Audio.cpp
index 8bd90ef..0610547 100644
--- a/Audio/Audio.cpp
+++ b/Audio/Audio.cpp
@@ -19,7 +19,14 @@
 
 #include "../Configuration.h"
 
+#ifndef MS_PLATFORM_WASM
 #include <bass.h>
+#else
+using HSTREAM = uint64_t;
+using HCHANNEL = uint64_t;
+using HSAMPLE = uint64_t;
+using DWORD = uint32_t;
+#endif
 
 #ifdef USE_NX
 #include <nlnx/audio.hpp>
@@ -71,8 +78,10 @@ namespace ms
 
 	Error Sound::init()
 	{
+#ifndef MS_PLATFORM_WASM
 		if (!BASS_Init(-1, 44100, 0, nullptr, 0))
 			return Error::Code::AUDIO;
+#endif
 
 		nl::node uisrc = nl::nx::Sound["UI.img"];
 
@@ -117,12 +126,18 @@ namespace ms
 
 	void Sound::close()
 	{
+#ifndef MS_PLATFORM_WASM
 		BASS_Free();
+#endif
 	}
 
 	bool Sound::set_sfxvolume(uint8_t vol)
 	{
+#ifndef MS_PLATFORM_WASM
 		return BASS_SetConfig(BASS_CONFIG_GVOL_SAMPLE, vol * 100) == TRUE;
+#else
+		return true;
+#endif
 	}
 
 	void Sound::play(size_t id)
@@ -130,8 +145,10 @@ namespace ms
 		if (!samples.count(id))
 			return;
 
+#ifndef MS_PLATFORM_WASM
 		HCHANNEL channel = BASS_SampleGetChannel((HSAMPLE)samples.at(id), false);
 		BASS_ChannelPlay(channel, true);
+#endif
 	}
 
 	size_t Sound::add_sound(nl::node src)
@@ -147,7 +164,11 @@ namespace ms
 			if (samples.find(id) != samples.end())
 				return 0;
 
+#ifndef MS_PLATFORM_WASM
 			samples[id] = BASS_SampleLoad(true, data, 82, (DWORD)ad.length(), 4, BASS_SAMPLE_OVER_POS);
+#else
+			samples[id] = 0;
+#endif
 
 			return id;
 		}
@@ -203,6 +224,7 @@ namespace ms
 
 		if (data)
 		{
+#ifndef MS_PLATFORM_WASM
 			if (stream)
 			{
 				BASS_ChannelStop(stream);
@@ -211,6 +233,7 @@ namespace ms
 
 			stream = BASS_StreamCreateFile(true, data, 82, ad.length(), BASS_SAMPLE_FLOAT | BASS_SAMPLE_LOOP);
 			BASS_ChannelPlay(stream, true);
+#endif
 
 			bgmpath = path;
 		}
@@ -229,6 +252,7 @@ namespace ms
 
 		if (data)
 		{
+#ifndef MS_PLATFORM_WASM
 			if (stream)
 			{
 				BASS_ChannelStop(stream);
@@ -237,6 +261,7 @@ namespace ms
 
 			stream = BASS_StreamCreateFile(true, data, 82, ad.length(), BASS_SAMPLE_FLOAT);
 			BASS_ChannelPlay(stream, true);
+#endif
 
 			bgmpath = path;
 		}
@@ -254,6 +279,10 @@ namespace ms
 
 	bool Music::set_bgmvolume(uint8_t vol)
 	{
+#ifndef MS_PLATFORM_WASM
 		return BASS_SetConfig(BASS_CONFIG_GVOL_STREAM, vol * 100) == TRUE;
+#else
+		return true;
+#endif
 	}
 }
\ No newline at end of file
