diff --git a/Util/NxFiles.cpp.rej b/Util/NxFiles.cpp.rej
new file mode 100644
index 0000000..4ac61a6
--- /dev/null
+++ b/Util/NxFiles.cpp.rej
@@ -0,0 +1,48 @@
+diff a/Util/NxFiles.cpp b/Util/NxFiles.cpp	(rejected hunks)
+@@ -18,20 +18,37 @@
+ #include "NxFiles.h"
+ 
+ #ifdef USE_NX
+-#include <fstream>
+-
+ #include <nlnx/node.hpp>
+ #include <nlnx/nx.hpp>
+ 
++#ifdef MS_PLATFORM_WASM
++#include <LazyFS/LazyFS.h>
++#else
++#include <fstream>
++#endif
++
+ namespace ms
+ {
+ 	namespace NxFiles
+ 	{
+ 		Error init()
+ 		{
++#ifdef MS_PLATFORM_WASM
++			// WASM Platform: Initialize LazyFS and register all .nx files for on-demand loading
++			LazyFS::Initialize();
++			for (auto filename : filenames)
++			{
++				std::string url = std::string("/assets/") + filename;
++				if (!LazyFS::RegisterFile(filename, url)) {
++					return Error(Error::Code::MISSING_FILE, filename);
++				}
++			}
++#else
++			// Native Platform: Check file existence
+ 			for (auto filename : filenames)
+ 				if (std::ifstream{ filename }.good() == false)
+ 					return Error(Error::Code::MISSING_FILE, filename);
++#endif
+ 
+ 			try
+ 			{
+@@ -53,4 +70,4 @@ namespace ms
+ 		}
+ 	}
+ }
+-#endif
+\ No newline at end of file
++#endif
