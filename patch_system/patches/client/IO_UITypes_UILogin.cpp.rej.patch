diff --git a/IO/UITypes/UILogin.cpp.rej b/IO/UITypes/UILogin.cpp.rej
new file mode 100644
index 0000000..dbf499e
--- /dev/null
+++ b/IO/UITypes/UILogin.cpp.rej
@@ -0,0 +1,32 @@
+diff a/IO/UITypes/UILogin.cpp b/IO/UITypes/UILogin.cpp	(rejected hunks)
+@@ -29,7 +29,9 @@
+ 
+ #include "../../Net/Packets/LoginPackets.h"
+ 
++#ifndef MS_PLATFORM_WASM
+ #include <windows.h>
++#endif
+ 
+ #ifdef USE_NX
+ #include <nlnx/nx.hpp>
+@@ -225,6 +227,8 @@ namespace ms
+ 				account.set_state(Textfield::State::FOCUSED);
+ 		};
+ 
++		LOG(LOG_INFO, "Trying to login with account: " << account_text << " and password length: " << password_text.length());
++
+ 		if (account_text.empty())
+ 		{
+ 			UI::get().emplace<UILoginNotice>(UILoginNotice::Message::NOT_REGISTERED, okhandler);
+@@ -273,7 +277,11 @@ namespace ms
+ 				return;
+ 		}
+ 
++#ifdef MS_PLATFORM_WASM
++		LOG(LOG_ERROR, "Buttons::BtResetPIC not implemented in WASM");
++#else
+ 		ShellExecuteA(NULL, "open", url.c_str(), NULL, NULL, SW_SHOWNORMAL);
++#endif
+ 	}
+ 
+ 	Button::State UILogin::button_pressed(uint16_t id)
