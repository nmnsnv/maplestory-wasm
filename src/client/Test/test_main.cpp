// Entry point for the native unit-test binary (Plan 00 / Plan 07).
// Defining this macro tells doctest to provide its own main().
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
